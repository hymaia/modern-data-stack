# generated by terraform from infra/templates/mlflow-values.yaml.tpl

mlflow:
  backendStore:
    databaseMigration: true
    databaseConnectionCheck: true
    postgres:
      enabled: true
      host: "mlflow.cnogqssgrgs3.eu-west-1.rds.amazonaws.com"
      port: 5432
      database: "mlflow"
    existingDatabaseSecret:
      name: "mlflow-postgresql-secret"
      usernameKey: "postgresql-username"
      passwordKey: "postgresql-password"

  auth:
    enabled: true
    adminUsername: "admin"
    adminPassword: ""
    existingAdminSecret:
      name: "mlflow-admin-secret"
      usernameKey: "username"
      passwordKey: "password"

  extraArgs:
    allowedHosts: "mlflow.fcussac.app.hymaia.com"
    uvicornOpts: "--log-level info"

  log:
    enabled: false

  extraEnvVars:
    MLFLOW_ALLOW_HOSTS: "mlflow.fcussac.app.hymaia.com"
    MLFLOW_TRACKING_USERNAME:
      valueFrom:
        secretKeyRef:
          name: mlflow-postgresql-secret
          key: postgresql-username
    MLFLOW_BACKEND_STORE_URI_PASSWORD:
      valueFrom:
        secretKeyRef:
          name: mlflow-postgresql-secret
          key: postgresql-password

  artifactRoot:
    s3:
      enabled: true
      bucket: "mlflow-artifacts-662195598891"

  serviceAccount:
    create: true
    annotations:
      eks.amazonaws.com/role-arn: "arn:aws:iam::662195598891:role/mlflow"

  ingress:
    enabled: true
    className: "alb"
    hosts:
      - host: "mlflow.fcussac.app.hymaia.com"
        paths:
          - path: /*
            pathType: ImplementationSpecific
    annotations:
      alb.ingress.kubernetes.io/group.name: shared-alb
      alb.ingress.kubernetes.io/scheme: internet-facing
      alb.ingress.kubernetes.io/target-type: ip
      alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS": 443}]'
      alb.ingress.kubernetes.io/certificate-arn: "arn:aws:acm:eu-west-1:662195598891:certificate/ce9e8ffd-1985-499b-999c-341218134e42"
